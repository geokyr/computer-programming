#include "pzhelp"
#include "cmath"

PROGRAM {

  int i, N, k;
  int a[10], b[8];

  N = READ_INT();

  for(i=0; i<=9; i++)
    a[i] = pow (i, N);

  for(k=0; k<=99999999; k++){
    b[0] = k/10000000;
    b[1] = (k%10000000)/1000000;
    b[2] = ((k%10000000)%1000000)/100000;
    b[3] = (((k%10000000)%1000000)%100000)/10000;
    b[4] = ((((k%10000000)%1000000)%100000)%10000)/1000;
    b[5] = (((((k%10000000)%1000000)%100000)%10000)%1000)/100;
    b[6] = ((((((k%10000000)%1000000)%100000)%10000)%1000)%100)/10;
    b[7] = ((((((k%10000000)%1000000)%100000)%10000)%1000)%100)%10;

    if (k == (a[b[0]] + a[b[1]] + a[b[2]] + a[b[3]] + a[b[4]] + a[b[5]] + a[b[6]] + a[b[7]]))
      WRITELN(k);
  }
}